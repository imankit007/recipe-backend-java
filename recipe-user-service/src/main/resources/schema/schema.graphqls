

type Query {
    lolomo: [ShowCategory]
}

type ShowCategory {
    name: String
    shows: [Show]
}

type Show {
    showId: Int
    title: String
    type: ShowType
}

enum ShowType{
     SERIES, MOVIE
}
